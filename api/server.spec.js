const request = require('supertest');

const server = require('./server');

describe(" ", () => {
    it(' ', async() => {
        const response = await request(server)
        expect(response)
    });
});